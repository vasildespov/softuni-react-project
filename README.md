# Description

This is a basic task manager built with MERN stack, Material UI and Styled Components

# How to run it
 
in client folder:

`npm install`
`npm run client`

in server folder:

`npm install`
`npm run server`

